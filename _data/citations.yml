# Citation counts, keyed by Google Scholar paper ID.
#
# Upstream ships this pre-populated with Einstein's Scholar profile; that data is
# removed. It stays empty until there is a Scholar profile to populate it from.
#
# al-folio's upstream `update-citations.yml` workflow refreshes this on a
# schedule. That workflow is not installed here — install it, and set
# `scholar_userid` in _data/socials.yml, if you want automatic citation badges.
metadata:
  last_updated:
papers: {}
